const FeedbackOptions = ({ options, onLeaveFeedback }) => {
  // return (
  // <div>
  //   {options.map(option => {
  //     console.log(option);
  //     return <button key={option.value} type="button"></button>;
  //   })}
  // </div>
  // );
};

export default FeedbackOptions;
